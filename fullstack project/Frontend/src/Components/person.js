const Person=(props)=>{
    return(
        <div>
            <h1>Student name is {props.name} rollno is {props.rollno} and branch is {props.branch} and campus is {props.campus}</h1>
        </div>
    )
}
export default Person